import { FilterProgramPipe } from './filter-program.pipe';

describe('FilterProgramPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProgramPipe();
    expect(pipe).toBeTruthy();
  });
});
